# TODO: Add a target to compile the document (currently reliant on VSCode LaTeX Workshop extension)
all:

clean:
	rm -f *.pdf *.out *aux *bbl *blg *log *toc *.ptb *.tod *.fls *.fdb_latexmk *.lof *.bcf *.run.xml *synctex.gz